#include "main.h"

/**
 * swap_int - swap a to b and b to a pointers
 * @a: first variable to swap
 * @b: second variable to swap
 */

void swap_int(int *a, int *b)
{
	int c;

	c = *a;
	*a = *b;
	*b = temp;
}
